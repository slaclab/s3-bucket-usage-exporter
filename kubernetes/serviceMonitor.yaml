apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: embargo-s3-exporter
  labels:
    release: sdf-k8s01
spec:
  selector:
    matchLabels:
      app: embargo-s3-exporter
  endpoints:
  - port: web
